# 更新日志

本文件记录项目的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.0.4] - 2026-09-23

### 修复

- **强调的定界符按 CommonMark 的规则判断**。此前只问「有没有配对的标记」，不问这个标记能不能开启或
  闭合强调，于是普通文字被改丑：
  - `2 * 3 * 4 = 6` 渲染成 `2 3 4 = 6`（星号被当作斜体标记吞掉），算式里的 `*` 跟着消失
  - `foo__bar__baz`、`x__y__z` 词内的双下划线被当成粗体，渲染成 `foobarbaz`（中间加粗）
  - `a * foo bar*`、`*foo bar *`、`** 空格在外 **` 这些没有合法配对的写法此前会误判

  现在按规范判断左右侧（flanking）：标记后面紧跟空格不能开启强调，前面紧跟空格不能闭合；下划线在词内部
  既不能开启也不能闭合。因此 `2 * 3 * 4` 是算式，`foo__bar__baz` 是标识符，而词首的 `__init__.py`
  仍然按规范加粗 `init`（这一点用参考实现核对过，不是猜的）

- **引用现在是容器，不再只是「带竖线的一行字」**。`>` 后面的内容剥掉一层标记后交给同一个块级解析器，
  于是一次修好四种情况：
  - 连续几行 `> ` 属于同一个引用（此前每行一个引用块，两条竖线中间留空隙）
  - `> > 嵌套` 真的嵌套（此前内层 `>` 直接显示成正文）
  - 引用里可以有列表、任务、有序列表、标题、代码块（此前 `> - [ ] 任务` 显示成字面 `- [ ] 任务`）
  - 引用里的任务复选框可点，写回的是笔记里正确的那一行（行号随 `>` 剥除而不漂移）

- **HTML 实体解码**：`&amp;` `&lt;` `&gt;` `&quot;` `&nbsp;` `&copy;` 等常见命名实体与
  `&#65;` / `&#x41;` 这类数字实体现在会解码成对应字符；表外的命名实体按规范保持原样。

- **HTML 标签不再被静默丢弃**：预览渲染不了 HTML，此前直接把 `<div …>` 从文字里抹掉，笔记里
  写的内容凭空少了一截。现在按 CommonMark 的做法把原始标签原样当文字显示，不渲染也不吞掉。

- **笔记库没有手动刷新的入口**。`RefreshRequested` 这个事件在笔记库侧一直没有任何地方发出，抽屉里那行
  「上次刷新」只是显示文字——别的应用（同步客户端、桌面编辑器）刚写进来的笔记要等应用重启才会出现。
  现在这一行可以点，点一下就重新扫描（标签也写明「点按刷新」）。

### 变更

- `MarkdownBlock.Quote` 从「深度 + 一行文字」变成「一组子块」，引用因此可以嵌套、可以装下列表与代码块；
  预览、纯文本投影与测试同步更新
- 行内解析器新增定界符的左右侧判断（`MarkdownSyntax.isPunctuation` 提供「Unicode 标点」这一定义）
  与 HTML 实体解码（常见命名实体 + 数字实体）

## [0.0.3] - 2026-09-23

### 修复

- **新建笔记在多数仓库里必然失败**。0.0.2 收敛重名逻辑时把「这个名字可以用」也当成了冲突：只是仓库里没有叫
  `未命名笔记.md` 的文件，就会直接抛「已存在同名项目」。于是从零建一个仓库、或者自己的笔记都不叫这个名字的人，
  点「新建」什么也不会发生；「重命名」改成任何没用过的名字同样失败。现在只在真的重名时才会往后加序号
  （`NoteNamingTest` 覆盖了这个契约，它此前没有测试）

- **附件目录不出现在文件夹树里**。目录树只能来自文件系统扫描，而「图片」按钮把图片放进 `attachments/` 时
  没有任何人去重新扫描，于是抽屉里没有这个目录的行，笔记里却已经存着一张放在那里的图。现在写入附件后会重新
  走一遍目录并发布，抽屉随即能看到它（无需手动刷新）

- **工具栏插入的图片不显示**。图片引用是写在光标处的，而光标通常在某一行的文字中间或末尾：`# 我的标题` 后面
  插入会得到 `# 我的标题![](attachments/1.jpg)`——图片落进了标题内部，而预览只把「独占一段」的图片画成图，
  行内图片一律显示成 `[alt]` 占位文字。两处都改了：引用总是独立成段（在文字中间插入会把段落拆开），行内图片
  也渲染成真正的图片（放不下就换行）。表格单元格里的图片保持占位文字，见「已知限制」

- **在笔记开头插入图片会把 front matter 顶下去**。打开笔记时光标就在正文最前面，此时按「图片」会把引用插在
  `---` 之前，`created` / `tags` / `favorite` 随即变成普通正文，笔记的元数据静默失效。插入时现在会跳过
  front matter，放在它下面

- **操作失败没有任何提示**。笔记库把「显示消息」这类效果直接丢掉了（`ShowMessage -> Unit`），所以新建、重命名、
  移动、刷新失败时界面毫无反应——「新建笔记」看起来就像个坏掉的按钮。现在失败会显示一条可点掉的消息（几秒后自动消失），
  且画在文件夹抽屉之上：最常失败的恰恰是抽屉里的操作（目录重名），放在抽屉下面等于没有

## [0.0.2] - 2026-09-23

### 新增

**编辑器**

- 预览里的任务复选框可以点了：点一下改写源码里对应那一行的 `- [ ]` / `- [x]`，走同一条自动保存
- 预览里的链接可以点了，交给系统打开（`http` / `https` / `mailto`）；源码里和预览里的裸链接
  （`https://…`、`www.…`）同样可以点
- 回车自动续写列表 / 任务 / 引用 / 有序列表，有序号自增；空项回车结束列表。手机输入法的回车
  不会以按键形式到达应用（IME 直接把换行写进文本域），所以这一步挂在文本变化上，而不是按键上
- Tab / Shift+Tab 对选中行缩进与反缩进一个单位（2 空格，与解析器一致）
- 格式工具条的「任务」按钮变成有状态动作：选中行已是任务时切换勾选状态，否则插成任务
- 预览支持 GFM 表格：`|---|---|` 对齐行决定列对齐，行会按表头列数补齐或截断

**Markdown 子系统**（新增 `markdown/` 与 `ui/markdown/` 两个专用目录）

- 纯逻辑全部进 `com.cycling.mynote.markdown/`（零 Compose、零 Android，可直接 JVM 单测）：
  语法、块级解析、行内解析、front matter、纯文本投影、源码编辑、工具条动作
- Compose 出口留在 `ui/markdown/`：预览渲染与源码高亮
- 新增 `MarkdownCommands`：工具条的九个动作变成纯函数，此前它们藏在 `EditorScreen` 里，无法测试

**图片与附件**

- 图片会真的显示出来（此前一律是 `[alt]` 占位）：仓库内相对路径的图片按笔记所在目录解析、经 SAF 读取并等比缩放；
  `http(s)` 图片联网取回。两者共用一套解码与内存缓存，读不到就退化成 alt 文字
- 为此应用新增唯一的权限 `INTERNET`，只在笔记里真的写了网络图片时才会发起请求
- 附件目录（Obsidian 里的 attachment folder）：工具条的「图片」按钮改为从系统相册选图，图片被复制进仓库里
  可配置的附件目录（默认 `attachments/`，第一次使用自动创建，重名自动加序号），笔记里只写入相对引用。
  原图不会被移动或删除

### 变更

**编辑器**（对齐设计稿更新后的编辑页）

- 视图模式切换移到标题栏右侧，编辑区不再是「统计行 + 切换器 + 正文」三段式，正文独占整块
- 去掉行数 / 字数统计行，以及独立的 YAML front matter 面板：front matter 本来就在源码里，
  标签、置顶、收藏直接改文本，不再有第二份元数据副本
- 去掉标题栏的 `⋯` 菜单，编辑器只负责写作。随之移除的有「立即保存」「收藏」「复制纯文本」
  「重命名」「删除」；重命名与删除仍在笔记库的长按操作条里，收藏改为在 front matter 里写
  `favorite: true`。自动保存关闭时，草稿在退出编辑时落盘
- 设置里的「字号」「行距」现在同时作用于源码区与预览（以设计稿的 13.5 / 21 为基准缩放，默认值下
  渲染结果不变）；「字体」明确只作用于界面与预览，编辑区按设计稿固定等宽，设置页里写明了这一点

**重构：把 Markdown 收敛成一处**

- 同一套语法此前散落在四个包里、最多被写 8 遍，并且已经开始互相矛盾。现在共用的正则与标记字面量
  都在 `MarkdownSyntax`，行内只有 `InlineMarkdownParser` 一份实现
- 删掉 `stripInlineMarkdown`（第二份行内解析器）：搜索结果里的正文改由真正的解析器提取，因此
  转义、行内代码、图片的处理与预览一致
- 合并三份纯文本投影（`MarkdownParser.toPlainText`、`stripInlineMarkdown`、以及
  `NoteIndexer.toPlainBody` 与 `NoteRepositoryImpl.plainBodyOf` 这两份同体函数）为 `MarkdownText`
- `InlineSpan` 现在带源码区间，语法高亮与预览读同一个解析结果，不会再出现「高亮以为是这样、
  渲染以为是那样」的分歧
- 删掉无引用的 `MatchText.toAnnotatedString` 与 6 个孤儿 `editor_*` 字符串

### 修复

- 命令面板里的「新建文件夹」不再凭空造一个叫「新建文件夹」的目录（第二次还会因为重名失败）：改为跳到笔记库、
  弹出与抽屉一致的命名对话框，建在仓库根目录
- 「新建笔记」统一建在仓库根目录，不再建到当前筛选的那个文件夹里；同一个抽屉里的两个「新建」此前行为不一致
- 键盘弹起时遮挡底部内容：应用是 edge-to-edge 的，系统不会为键盘缩小窗口，此前没有任何界面消费
  `WindowInsets.ime`，于是编辑器的格式工具条、搜索页的底部导航、文件夹树抽屉的操作行都被键盘盖住。
  现在由 `MyNoteScreen` 统一处理 IME 内边距（抽屉另算，它不在这个框架里）
- 文件夹树抽屉的面板此前没有覆盖状态栏：它把状态栏内边距加在了「面板表面」上，于是表面从状态栏下面
  才开始，顶部露出的是背后的蒙层。现在表面（含阴影）与内边距分层——表面覆盖到状态栏与屏幕底边，
  内边距只作用在抽屉自己的内容上
- 文件夹树的展开 / 折叠此前完全无效：`treeEntries` 只在仓库流回调里构造一次，切换展开集合只改了状态、
  没有重建行，因此点开箭头看不到任何变化。现在展开集合本身是一条流，和笔记列表一样参与推导，
  任一变化都会重新展平。顺带把箭头的点击区域从 13dp 的图标扩大到「箭头 + 文件夹图标」整块，
  此前绝大多数点按都落到行上（那会筛选列表），看起来就像收不起来
- 抽屉里的文件夹数量此前来自上一次文件系统扫描，而它下面列出的行来自索引：扫描之后新建的笔记会
  出现「文件夹显示 0、展开却有一个文件」。数量现在按索引里的笔记递归统计，与展开后能看到的行一致
- 设置里的「软换行」此前完全没人读（死开关）。关闭后源码区改为横向滚动而不折行
- 源码着色与解析对齐：`~~删除线~~`、Setext 标题（`===`）、分隔线此前都不高亮；行内代码里的
  `**` 会被误加粗、`snake_case` 会被误当斜体。高亮现在由解析器的结果驱动，这类分歧不可能再出现


## [0.0.1] - 2026-09-22

首个版本。功能与界面对齐 Pencil 设计稿，全部屏幕为原生 Compose 实现。

### 新增

**仓库**
- 通过系统文件夹选择器授权一个文件夹作为笔记仓库，持久化授权，重启后自动恢复
- 默认位置 `Documents/Notes`：自动创建目录并把选择器预导航到该位置，只需确认一次
- 切换 / 断开仓库（断开只解除本机授权，不删除任何文件）
- 首次使用可一键生成示例内容（`Inbox.md` 与 `日记/`）

**笔记库**
- 列出仓库内全部 `.md` / `.markdown` 文件，显示标题、摘要、相对时间、标签
- 筛选：全部 / 最近 / 标签 / 收藏 / 文件夹
- 排序：最近更新、最早更新、标题、创建时间；置顶笔记恒在最前
- 长按操作：置顶、重命名、移动、删除
- 空仓库引导：创建第一篇笔记，或创建示例内容

**编辑器**
- 三种视图模式：编辑（Markdown 语法着色源码）、分屏、预览
- 语法着色基于恒等偏移映射的 `VisualTransformation`，不影响光标与选区
- 断点自动保存（900ms），可关闭；关闭后可从 `⋯` 菜单手动保存
- 格式工具条：标题、加粗、斜体、无序 / 任务 / 有序列表、引用、代码、链接、图片，作用于当前选区
- YAML front matter 面板：标签增删、置顶开关，并显示 `YAML` 标识
- 行数与字数统计、文件名、保存状态指示
- `⋯` 菜单：复制纯文本、收藏、重命名、删除

**搜索与命令面板**
- 单一输入框同时承担搜索与命令面板：输入 `>` 切换
- 按命中字段排序（标题 → 标签 → 正文），命中片段高亮
- 结果标注 `标题 / 内容 / 标签` 与所在文件夹，支持「查看全部」
- 命令：新建笔记、新建日记、新建文件夹、重建搜索索引、刷新笔记库、快速捕获、切换主题、打开设置
- 记录最近搜索与最近使用的标签

**文件夹树**
- 抽屉式，可展开 / 折叠，显示每层笔记数
- 显示上次刷新时间与外部修改数量（被其他应用或同步工具改动的文件）
- 新建文件夹、新建笔记
- 折叠状态下可直接筛选到某个文件夹

**设置**
- 仓库信息与本地文件用量
- 主题：浅色 / 深色 / 跟随系统
- 编辑器：字体、字号、行距、软换行、自动保存
- 索引：全量重建、增量刷新（按修改时间与大小只重读变化部分）
- 关于：版本、设计说明

**快速捕获**
- 注册为系统分享目标（`ACTION_SEND`）与划词菜单项（`ACTION_PROCESS_TEXT`）
- 收到文字后可编辑，再选择追加到 `Inbox.md` 或新建笔记

**日记**
- 按 `日记/yyyy-MM-dd.md` 约定，按月分组展示
- 一键打开今天的日记，不存在则按模板创建

**架构与工程**
- MVI：单一不可变状态对象 + 一次性副作用走 `Channel`
- 分层：`core`（纯 Kotlin）/ `data` / `domain`（接口）/ `ui`，Android 依赖只出现在 data 与 ui
- Hilt 依赖注入，Dispatcher 与协程作用域走限定符注入以便测试替换
- Room 索引（可删除、可重建）、DataStore 偏好
- SAF 访问基于 `DocumentsContract`，一次查询列出目录子项
- 笔记以仓库内相对路径为标识，重新授权同一文件夹后索引仍然有效
- 64 个 Lucide 图标由 `tools/icons/gen-icons.js` 生成为 `ImageVector`
- 82 个 JVM 单元测试，覆盖 front matter、Markdown 解析、相对时间、字节数、文件夹树与图标构建
- GitHub Actions：提交与 PR 时运行单元测试，并构建调试包与未签名的正式包（用于验证 R8 配置）

**签名与正式包**
- 正式构建开启 R8（压缩 / 混淆 / 优化）与资源压缩，包体从 36 MB 降至约 2.9 MB
- 签名配置从仓库外读取：`keystore.properties`（已忽略）指向仓库外的 keystore；
  缺少该文件时正式包仍可构建，只是不签名
- 提供 `keystore.properties.example` 与 `app/proguard-rules.pro`

### 修复

- 笔记库：筛选标签行紧贴搜索框、`最近更新 / 共 N 篇` 表头上下间距过窄，已按设计稿的 18dp 节奏对齐
- 笔记库：仓库为空时不再显示 `共 0 篇` 的排序表头，空状态改为在剩余空间垂直居中
- 编辑器：`⋯` 菜单与下方编辑区之间补齐间距，此前紧贴在一起
- 底部导航：下边距按设计稿由 10dp 调整为 14dp
- 空状态：内边距与元素间距按设计稿调整为 40/32 与 20dp
- 搜索：命令面板列表改为在限高容器内滚动，此前依赖父容器的无界高度
- 设置：卡片改用共用的 `MyNoteCard`，去掉与该组件重复的内联实现
- 清理 6 个未被引用的组件（`MyNoteLabeledDivider`、`NoteMetaRow`、`MyNoteCaret`、
  `MyNoteHairline`、`PreviewCheckbox` 等）与 7 个未使用的尺寸 token

[Unreleased]: https://github.com/mariamjensen42-glitch/MyNote/compare/v0.0.4...HEAD
[0.0.4]: https://github.com/mariamjensen42-glitch/MyNote/compare/v0.0.3...v0.0.4
[0.0.3]: https://github.com/mariamjensen42-glitch/MyNote/compare/v0.0.2...v0.0.3
[0.0.2]: https://github.com/mariamjensen42-glitch/MyNote/compare/v0.0.1...v0.0.2
[0.0.1]: https://github.com/mariamjensen42-glitch/MyNote/releases/tag/v0.0.1
